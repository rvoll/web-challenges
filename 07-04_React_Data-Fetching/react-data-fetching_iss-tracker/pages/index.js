// You will need to fetch on interval and refetch on click of a button.
// Switch to the ISSTracker component:

import ISSTracker from "../components/ISSTracker";

export default function App() {
  return <ISSTracker />;
}
