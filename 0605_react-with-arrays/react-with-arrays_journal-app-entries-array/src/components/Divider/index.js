import "./Divider.css";

export default function Divider() {
  return <hr className="divider" role="none" />;
}

// export default function Divider({ id }) {
//   return `${id} === 997` ? "" : <hr className="divider" role="none" />;
// }
