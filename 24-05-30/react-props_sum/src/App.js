import React from "react";
import "./styles.css";

export default function App() {
  return (
    // <h1>Replace me with your component!</h1>;
    <div>Sum(1 2)</div>
  );
}

// pass two props to a component
// and use them to calculate the sum

// It accepts two props called valueA and valueB
// (make sure to destructure them).
// It returns an HTML element and uses both props to
// dynamically render something like "1 + 2 = 3" (according to the props).
// Inside of the return statement of your App component,
// replace the heading with your Sum component and pass it
// both props with values of your choice.

// function Sum({valueA valueB}) {
// return (
//   <div>
//     {valueA} " + " {valueB} = {valueA}+{valueB}
//   </div>
// )
// }
// HIER WEITER
